@model IEnumerable<MVCBiblioteka.Models.Category>
@{
    ViewBag.Title = "Lista gatunków";
}

    <p>
        <h2>Ilość gatunków: @Model.Count()</h2>
    </p>
    <ul>
        @foreach (var genre in Model)
        {
            <li>@Html.ActionLink(genre.name, "Browse", new { genre = genre.name })</li>
        }
    </ul>